{
  "name": "blog_website-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute blog-db --file=./schema.sql"
  },
  "dependencies": {
    "wrangler": "^3.80.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@cloudflare/workers-types": "^4.20241106.0"
  }
}