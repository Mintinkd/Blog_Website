node_modules

# 本地 agent 记忆与临时产物，不进 git
.workbuddy/
build_tmp.log

# worker 自动部署时 Cloudflare 自己 build 生成，不进 git
worker/dist/

# wrangler 本地构建缓存（dev/deploy 时生成），不进 git
.wrangler/
worker/.wrangler/

# Astro dev/build 缓存（data-store、settings、临时类型），不进 git
.astro/
